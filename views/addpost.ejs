<%-include('layouts/header') %>
    <%-include('layouts/navbar') %>
        <% if(error) {%>
            <div class="alert alert-error shadow-lg" style="width: 30%; margin-left:50px; margin-top:40px;">
                <div>
                    <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none"
                        viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <span><%=error %> </span>
                </div>
            </div>
            <% } else {%>
                <h3>s</h3>
                <% } %> 
                <div>
                    <form method="POST" id="form" action="/post/add" class="form-control">
                        <label class="input-group input-group-lg">
                            <input id="input" type="title" name="title" placeholder="Title"
                                class="input input-bordered input-lg" />
                        </label>
                        <textarea id="text" name="text" class="textarea textarea-primary"
                            placeholder="descriptioin..."></textarea>
                        <button type="submit" class="btn btn-outline">Create</button>
                    </form>


                </div>

                <%-include('layouts/footer') %>

                    <style>
                        #form {
                            display: flex;
                            justify-content: space-between;
                            margin: 40px;
                        }

                        #input {
                            width: 40%;
                            margin-bottom: 20px;
                        }

                        #text {
                            width: 40%;
                            margin-bottom: 20px;
                        }

                        button {
                            width: 40%;
                        }
                    </style>